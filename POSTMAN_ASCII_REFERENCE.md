# Postman Testing - ASCII Visual Reference

## Test 1: Login Request

```
┌─────────────────────────────────────────────────────────────┐
│ 🔵 POST  http://localhost:8080/api/auth/login              │
├─────────────────────────────────────────────────────────────┤
│ [ Params ] [ Headers ] [ Body ] [ Scripts ] [ Settings ]    │
├─────────────────────────────────────────────────────────────┤
│ HEADERS:                                                     │
│ ┌────────────────┬──────────────────┐                       │
│ │ Content-Type   │ application/json  │                       │
│ └────────────────┴──────────────────┘                       │
│                                                              │
│ BODY (raw, JSON):                                           │
│ {                                                            │
│   "username": "testuser",                                   │
│   "password": "password123",                                │
│   "role": "USER"                                            │
│ }                                                            │
├─────────────────────────────────────────────────────────────┤
│ [blue] SEND [v]                                             │
├─────────────────────────────────────────────────────────────┤
│ ✅ 200 OK  |  200 ms  |  156 B                              │
├─────────────────────────────────────────────────────────────┤
│ RESPONSE:                                                    │
│ {                                                            │
│   "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJz..."  │
│ }                                                            │
└─────────────────────────────────────────────────────────────┘

ACTION: Copy the token value
```

---

## Test 2: GET /hello WITHOUT Token (Should Fail)

```
┌─────────────────────────────────────────────────────────────┐
│ 🟢 GET  http://localhost:8080/hello                        │
├─────────────────────────────────────────────────────────────┤
│ [ Params ] [ Headers ] [ Body ] [ Scripts ] [ Settings ]    │
├─────────────────────────────────────────────────────────────┤
│ HEADERS:                                                     │
│ ┌────────────────┬──────────────────┐                       │
│ │ (empty)        │                  │                       │
│ └────────────────┴──────────────────┘                       │
│                                                              │
│ BODY: (none - it's a GET request)                          │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│ [blue] SEND [v]                                             │
├─────────────────────────────────────────────────────────────┤
│ ❌ 403 Forbidden  |  45 ms  |  0 B                          │
├─────────────────────────────────────────────────────────────┤
│ RESPONSE:                                                    │
│ (empty)                                                      │
│                                                              │
│ ✅ EXPECTED: 403 Forbidden (no token provided)             │
└─────────────────────────────────────────────────────────────┘
```

---

## Test 3: GET /hello WITH Valid Token (Should Succeed)

```
┌─────────────────────────────────────────────────────────────┐
│ 🟢 GET  http://localhost:8080/hello                        │
├─────────────────────────────────────────────────────────────┤
│ [ Params ] [ Headers ] [ Body ] [ Scripts ] [ Settings ]    │
├─────────────────────────────────────────────────────────────┤
│ HEADERS:                                                     │
│ ┌─────────────────┬──────────────────────────────────────┐ │
│ │ Authorization   │ Bearer eyJhbGciOiJIUzI1NiIsInR5cCI... │ │
│ │ Content-Type    │ application/json                     │ │
│ └─────────────────┴──────────────────────────────────────┘ │
│                                                              │
│ BODY: (none - it's a GET request)                          │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│ [blue] SEND [v]                                             │
├─────────────────────────────────────────────────────────────┤
│ ✅ 200 OK  |  52 ms  |  42 B                                │
├─────────────────────────────────────────────────────────────┤
│ RESPONSE:                                                    │
│ "Welcome to Spring Security with JWT"                      │
│                                                              │
│ ✅ SUCCESS: Authenticated with JWT!                        │
└─────────────────────────────────────────────────────────────┘
```

---

## Test 4: GET /hello WITH Invalid Token (Should Fail)

```
┌─────────────────────────────────────────────────────────────┐
│ 🟢 GET  http://localhost:8080/hello                        │
├─────────────────────────────────────────────────────────────┤
│ [ Params ] [ Headers ] [ Body ] [ Scripts ] [ Settings ]    │
├─────────────────────────────────────────────────────────────┤
│ HEADERS:                                                     │
│ ┌─────────────────┬──────────────────────────────────────┐ │
│ │ Authorization   │ Bearer invalid.token.here            │ │
│ └─────────────────┴──────────────────────────────────────┘ │
│                                                              │
│ BODY: (none - it's a GET request)                          │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│ [blue] SEND [v]                                             │
├─────────────────────────────────────────────────────────────┤
│ ❌ 403 Forbidden  |  38 ms  |  0 B                          │
├─────────────────────────────────────────────────────────────┤
│ RESPONSE:                                                    │
│ (empty)                                                      │
│                                                              │
│ ✅ EXPECTED: 403 Forbidden (invalid token)                │
└─────────────────────────────────────────────────────────────┘
```

---

## Step-by-Step: Setting Up In Postman

### 1. Import Collection

```
File → Import
  └─ Select: Spring-Security-JWT-Postman-Collection.json
  └─ Click: Import
```

### 2. View Imported Requests

```
Sidebar (Left):
├─ Spring Security JWT Demo
│  ├─ Authentication
│  │  └─ Login - Get JWT Token
│  │
│  └─ Secured Endpoints
│     ├─ GET /hello (WITHOUT Token)
│     ├─ GET /hello (WITH Valid Token)
│     └─ GET /hello (WITH Invalid Token)
```

### 3. Run Test 1: Login

```
Click: "Login - Get JWT Token"
  └─ Review body (correct values?)
  └─ Click SEND
  └─ Look for: 200 OK in response
  └─ Copy: token value from response
```

### 4. Set Environment Variable (Optional)

```
In "Login - Get JWT Token" request:
  └─ Go to: Tests tab
  └─ Code should auto-save token to environment
  └─ Or manually: pm.environment.set('jwt_token', jsonData.token);
```

### 5. Run Test 3: GET /hello WITH Token

```
Click: "GET /hello (WITH Valid Token)"
  └─ Check: Authorization header has correct format
  └─ If no token: Replace {{jwt_token}} with copied token
  └─ Click SEND
  └─ Look for: 200 OK ✅
```

---

## Common Postman Elements Reference

### Status Codes & Colors

```
🟢 200 OK              = Success (green)
🟡 201 Created         = Success (green)
🔵 400 Bad Request     = Client error (orange)
🔴 401 Unauthorized    = Auth required (red)
🔴 403 Forbidden       = Auth failed (red)
🔴 404 Not Found       = Endpoint missing (red)
⚫ 500 Server Error     = Server error (red)
```

### Headers Format in Postman

```
Tab: Headers
┌─────────────────────┬──────────────────────┐
│ Key                 │ Value                │
├─────────────────────┼──────────────────────┤
│ Authorization       │ Bearer <token>       │
│ Content-Type        │ application/json     │
└─────────────────────┴──────────────────────┘
```

### Body Format in Postman

```
Tab: Body
  ◉ form-data
  ◉ x-www-form-urlencoded
  ◉ raw           ← SELECT THIS
      ▼ JSON      ← SELECT THIS

{
  "key": "value"
}
```

### Authorization Helper (Alternative)

```
Tab: Authorization
  ▼ Type: [Bearer Token▼]
  Token: [paste_token_here]

This auto-generates the Authorization header
```

---

## Verification Checklist

### After Test 1 (Login)

```
✓ Status shows 200 OK (green)
✓ Response contains "token" field
✓ Token is a long string starting with "eyJ"
✓ Can copy token value without errors
```

### After Test 2 (GET /hello without token)

```
✓ Status shows 403 Forbidden (red)
✓ No Authorization header in request
✓ Response body is empty or contains error
```

### After Test 3 (GET /hello with token)

```
✓ Status shows 200 OK (green)
✓ Authorization header present with "Bearer " prefix
✓ Response body contains: "Welcome to Spring Security with JWT"
✓ This proves JWT authentication works!
```

### After Test 4 (GET /hello with invalid token)

```
✓ Status shows 403 Forbidden (red)
✓ Authorization header present but with invalid token
✓ Response body is empty or contains error
✓ This proves invalid tokens are rejected!
```

---

## Token Inspection (Advanced)

To inspect your JWT token details:

1. Copy the token from Test 1 response
2. Visit: https://jwt.io/
3. Paste token in "Encoded" field
4. See decoded payload (Payload section)

```
Header:
{
  "alg": "HS256",
  "typ": "JWT"
}

Payload:
{
  "sub": "testuser",
  "roles": "USER",
  "iat": 1736614420,    (issued at)
  "exp": 1736618020     (expires at)
}

Signature:
(verified with your secret)
```

---

## Keyboard Shortcuts (Postman)

```
Ctrl/Cmd + Shift + N     = New request
Ctrl/Cmd + Enter         = Send request
Ctrl/Cmd + S             = Save request
Tab / Shift+Tab          = Switch between sections
Ctrl/Cmd + Alt + C       = Copy request as cURL
```

---

## Next Steps After Successful Testing

1. ✅ Verify all 4 tests pass
2. ✅ Understand the request/response flow
3. ✅ Create additional secured endpoints
4. ✅ Implement role-based authorization
5. ✅ Add database integration for real users
6. ✅ Implement token refresh mechanism
7. ✅ Add logout functionality

---

**You're ready to test! 🚀**

